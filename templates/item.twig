<div class="item-container">
    <div class="item" id="{{ item.id }}">
        <h5>
            {% if item.title is empty %}
                {{ item.title }}
            {% else %}
                <a href="/post/{{ item.title }}/">{{ item.title }}</a>
            {% endif %}
            {% include "widgets/postVote.twig" %}
        </h5>

        <h4>
            {{ item.photographer }}
        </h4>

        <div class="image-holder">
            {% if item.zoomable %}
                <a href="/post/{{ item.id }}">
                    <img src="{{ item.tmb }}" title="{{ item.title }}" alt="{{ item.title }}"/>
                </a>
            {% else %}
                <img src="{{ item.image }}" title="{{ item.title }}" alt="{{ item.title }}"/>
            {% endif %}
        </div>
        <noindex>
            <div class="permalink"><a href="/post/{{ item.id }}">permalink</a></div>
        </noindex>
        <div class="tags">{% include "tagsList.twig" %}</div>
        <div class="date">{{ item.date }}</div>
        <div class="clear"></div>
    </div>
</div>
